er=read_xlsx('communitySmellsDataset.xlsx')
setwd("C:/Users/franc/OneDrive/Desktop")
setwd("C:/Users/franc/OneDrive/Desktop/csDetector-master")
er=read_xlsx('communitySmellsDataset.xlsx')
we=er[,5:14]
we$OSE=as.double(we$OSE)
we$PDE=as.double(we$PDE)
we$BCE=as.double(we$BCE)
we$SV=as.double(we$SV)
we$OS=as.double(we$OS)
we$SD=as.double(we$SD)
we$RS=as.double(we$RS)
we$TF=as.double(we$TF)
we$UI=as.double(we$UI)
we$TC=as.double(we$TC)
ggplot(we, aes(x = we$OSE, y = we$BCE)) + geom_point()
ggplot(we, aes(x = OSE, y = BCE)) + geom_point()
modello <- lm(variabile2 ~ variabile1, data = dataset)
modello <- lm(BCE ~ OSE, data = we)
summary(modello)
cor(OSE,BCE)
cor(we$OSE,we$BCE)
